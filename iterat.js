
const numbers = [12,40,50,20];
numbers.forEach((val, index, ar) => {
    console.log(`Value: ${val} index: ${index} array: ${ar}`);
});

console.log("Starting map functioning! and new array with modified Aarray!");

const res = numbers.map((val,index, ar) => {
    return val * 10;
    const filarr = numbers.filter((va))
})
console.log(`Result map: ${res}`);

console.log(`This is filter method and new array!`);


